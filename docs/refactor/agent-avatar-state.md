# Agent Avatar State — Phase 3 Audit

_This file is generated by `scripts/audit-agent-avatars.ts`._

Run it against the target environment before applying the backfill migration:

```bash
SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... pnpm tsx scripts/audit-agent-avatars.ts
```

The script is read-only and safe to run against production. It overwrites this file with
a markdown table covering every row in `public.agent_definitions`, plus the joined
`public.users` snapshot, and flags rows where `agent_definitions.avatar_url IS NULL`.

> Placeholder contents — replace by running the script and pasting the resulting table
> into the Phase 3 PR description before merging the backfill migration.
