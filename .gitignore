node_modules/
# Accidental local pnpm content-addressable store (never commit)
.pnpm-store/
# TypeScript incremental build info
*.tsbuildinfo

build/
dist/
.next
.vercel/
playwright-report/
test-results/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Root-level Deno lock from ad-hoc `deno` at repo root; CI uses supabase/deno.lock
/deno.lock

# Supabase CLI local state
supabase/.temp/

# Agent/local checklist runner output (pnpm run check:report)
tmp/

# Local symlink to agent skills (see skills-lock.json); not portable
skills/
next-env.d.ts

# Copilot suggestion ignored: do not add a one-off filename rule for deleted scratch files; rely on review and Husky instead of bloating .gitignore.

# Local Cursor plan drafts (tracked: .cursor/rules/, .cursor/settings.json)
.cursor/plans/
